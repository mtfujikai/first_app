<h1>トップページ</h1>
<%= link_to '新規投稿', '/posts/new' %>
 <%= link_to 'トップページに戻る' '/posts'%>
<% @posts.each do |post| %>
    <div class='post'>
        <div class = 'post-date'>
            投稿日時<%= post.created_at %>
        </div>
        <div class = 'post-content'>
            <%= post.content %>
        </div>
    </div>
<% end %>
 
posts.each do |post| posts内で取り扱っている情報を全て一つ一つ|post|として取り扱う
    puts post.content posts内の全ての情報(post)のcontentを表示する
end